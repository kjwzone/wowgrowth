name: Deploy frontend-v15

on:
  push:
    branches: [main]
    paths:
      - "frontend-v15/**"
      - ".github/workflows/deploy-frontend-v15.yml"
  workflow_dispatch:

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
  VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend-v15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend-v15/package-lock.json

      - name: Install
        run: npm ci

      - name: Pull Vercel project settings
        run: npx vercel@latest pull --yes --environment=production --token="$VERCEL_TOKEN"

      - name: Build
        run: npm run build

      - name: Prepare Vercel prebuilt output
        run: node scripts/prepare-vercel-output.mjs

      - name: Deploy prebuilt output to Vercel
        id: deploy
        run: |
          set -euo pipefail
          npx vercel@latest deploy --prebuilt --prod --yes --token="$VERCEL_TOKEN" 2>&1 | tee deploy.log
          url="$(tail -1 deploy.log)"
          echo "url=$url" >> "$GITHUB_OUTPUT"
          echo "Production URL: $url"
